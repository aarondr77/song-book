/**
 * Storage configuration constants
 * Update these values in one place to change limits across the application
 */

// Video file size limit in bytes (2GB)
export const VIDEO_FILE_SIZE_LIMIT = 2 * 1024 * 1024 * 1024; // 2GB

// Video file size limit formatted for display
export const VIDEO_FILE_SIZE_LIMIT_GB = 2;

// Image file size limit in bytes (5MB)
export const IMAGE_FILE_SIZE_LIMIT = 5 * 1024 * 1024; // 5MB
